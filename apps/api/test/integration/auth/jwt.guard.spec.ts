/**
 * Integration tests — JwtAuthGuard com JWT real.
 * Fonte: docs/backend/14-tests.md
 */
describe.skip('JwtAuthGuard (integration)', () => {
  it('should allow valid JWT', async () => {});
  it('should reject expired JWT', async () => {});
  it('should reject malformed JWT', async () => {});
  it('should allow @Public() routes without JWT', async () => {});
});
