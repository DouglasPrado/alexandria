/**
 * E2E tests — Recovery via seed phrase.
 * Fonte: docs/backend/14-tests.md
 *
 * Fluxo: create cluster → upload file → POST /api/clusters/recovery → verify data intact
 */
describe.skip('Recovery E2E', () => {
  it('should recover cluster from seed phrase and report vaults/manifests', async () => {});
  it('should reject invalid seed phrase with 422', async () => {});
});
