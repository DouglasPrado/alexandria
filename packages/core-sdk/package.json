{
  "name": "@alexandria/core-sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Alexandria Core SDK — cryptography, chunking, hashing, BIP-39",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:unit": "jest",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
